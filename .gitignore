# =======================
# Cloud Compliance Project .gitignore 
# Prevent unnecessary files from being tracked by Git
# and facing the challenge of uploading large files to GitHub as experienced
# in the Ollama project.
# =======================

# Ignore Ollama models and data (prevents model files from being pushed)
ollama-data/
ollama/
models/
*.bin
*.pt
*.ckpt

# Python virtual environments and caches
.venv/
__pycache__/
*.pyc
*.pyo

# Environment variable files (commonly used for secrets)
.env

# Log files (prevents clutter)
*.log

# Node.js, React, Vite, and other JS build artifacts (future-proofing)
node_modules/
dist/
build/
